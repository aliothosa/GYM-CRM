package com.elioth.epam.gymcrm.auth;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

/**
 * Optional/low-priority: enum with no custom logic.
 */
@Disabled("Optional/low-priority enum coverage")
class RoleTest {

    @Test
    @Disabled("Practice skeleton")
    void shouldContainTraineeAndTrainerValues() {
        // Assert
        // TODO: assertEquals(2, Role.values().length)
        // TODO: assertEquals(Role.TRAINEE, Role.valueOf("TRAINEE"))
        // TODO: assertEquals(Role.TRAINER, Role.valueOf("TRAINER"))
    }
}
